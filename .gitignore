### Gradle ###
.gradle
build/
!gradle/wrapper/gradle-wrapper.jar
!**/src/**/build/

### IntelliJ IDEA ###
.idea/
*.iws
*.iml
*.ipr
out/
!**/src/**/out/

.run/
# 在仓库根目录跑 gradlew 时 log4j 会在这儿建一个空的 logs/，别让它进版本库
logs/

### Eclipse ###
.apt_generated
.classpath
.eclipse/
.factorypath
.project
.settings
.springBeans
.sts4-cache
bin/
!**/src/**/bin/

### VS Code ###
.vscode/

### Mac OS ###
.DS_Store

### Minecraft Modding ###
run/
!**/src/**/run/
**/src/generated/**/.cache/
repo/
!**/src/**/repo/

### 本机诊断素材 ###
# 崩溃报告包、外部模组源码这类查问题时丢进来的东西：体积大（动辄数 MB 的
# zip）、只对当时那一次排查有意义，进了仓库就永远躺在历史里删不掉。
# 目录保留在本机，git 不再盯着它。
上传/

# 自己测试用的壁纸，同一个道理：拿来试渲染的图，不是模组的一部分。
# 它曾经被 git add -A 顺手收进一个不相干的提交里，那次是从历史里剜出去的。
#
# 必须写成 **/ 开头：带斜杠的模式是【锚在仓库根】的，而各版本的工程现在都在
# 「MCphone <加载器> <版本>/」这样的子目录下。写成 docs/wallpapers/ 的话，
# 这条规则一个也匹配不上，上面那次事故会原样重演一遍
**/docs/wallpapers/

### Dev run dir ###\nrun/
